test()
{
    x = a | b & c;
    x = a & b | c & d;
    x = a | b | c;
    x = a < b < c;
    x = a < b == c < d;
    x = a < b * c == c + d < e * f
    x = a * b / d % e

}

//@PRACOWNIA
//@stop_after parser
